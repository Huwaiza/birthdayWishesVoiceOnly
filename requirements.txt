# Birthday Song Generator v7 — ACE-Step pipeline
# Install ACE-Step itself separately from git:
#   git clone https://github.com/ace-step/ACE-Step.git external/ACE-Step
#   pip install -e external/ACE-Step
# (ACE-Step is not on PyPI as a current release.)

# These are the deps our own code (song/, generate_birthday.py, batch_render.py) needs
# on top of whatever ACE-Step pulls in. Versions left loose so we share with ACE-Step's pins.
numpy
soundfile
pydub
# torch / torchaudio come from ACE-Step's requirements.txt — don't pin here

# Test deps
pytest
